{"version":"1.0.9","name":"TradeCore ERP","date":"2026-09-12","description":"v1.0.9 build 2026-09-08-25 SUPERADMIN MULTI-COMPANY CRUD SCOPE PERSISTENCE: (1) Req 1c — DYNAMIC ACTIVE-COMPANY SUBMISSION GATE: the atomic direct-MySQL flush now resolves the active company AT SUBMISSION TIME (getActiveCompanyScope from live currentCompanyId + session user, not a stale form closure) and REJECTS the post with a clear red toast (Save blocked: no active company selected) when no scope is bound and the changed records also carry no per-record company_id — blocking silent writes into a phantom '' scope that no device could read; companies (self-scoped by id) and categories (self-scoped via their co_<id>:<name> string prefix) are exempt. (2) Req 2 — ARRAY-INDEX CRASH REMOVAL: full-source audit of every [1]-indexed read (filter/map/reduce/match/split) shows only guarded sites remain; the last unguarded Array.filter [1] reader (RootTraReportsPanel) was fixed in build 24 and v2ListCategories now guards null/hole rows BEFORE reading tuple index 1. (3) Req 3 verified — OPTIMISTIC PIN: applyData overlays in-flight directDeltaGuards snapshots over stale remote arrays (App.tsx 2306-2316) plus waitForDirectDeltas on company switch, so a background poll/SSE arriving mid-delta can no longer wipe a just-added local record before MySQL settles. (4) Req 4 verified — UNIVERSAL DELETE: v2_delete_store/branch/category/user_account handlers soft-delete under the target company_id with tcGuardUserMutation super-admin gate, tcBumpMainStateVersion, blob merge + audit; payloads inject company_id via companyKey spread in v2UpsertStore/Branch/Category/UserAccount. SuperAdmin store/branch/category/user CRUD now persists to the correct company on every device, survives refresh, and can never silently target the wrong/empty scope."}