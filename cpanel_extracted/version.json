{"version":"1.0.9","name":"TradeCore ERP","date":"2026-09-13","description":"v1.0.9 build 2026-09-08-30 DATA PERSISTENCE FIX: (1) get_state Path B now calls tcAssembleDynamicSnapshot even when the tradecore_system_state blob doesn't exist — first refresh after fresh deploy or cleared blob no longer returns empty arrays. (2) save_state overlays MySQL-assembled branches/stores/categories into the blob before writing, so a single-client flush never clobbers data written by another client via v2_* atomic endpoints."}