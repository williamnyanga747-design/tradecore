{"version":"1.0.9","name":"TradeCore ERP","date":"2026-09-10","description":"v1.0.9 build 2026-09-08-14 COMPANY-ADD FULL FIX: Backend v2_upsert_company now robust — accepts entity/company/bare payload, both camelCase and snake_case, auto-generates id when missing, catches PDOException (never bare false), structured error response. tcUpsertCompanyRow stores theme_color/subscription_end/logo + normalizes tin/tin_number/language/locale. companies table ALTERed with theme_color/subscription_end/logo columns. tcLoadCompanies returns themeColor/subscriptionEnd/logo, ORDER BY created_at DESC. Frontend addCompany uses normalizeCompanyPayload (snake_case, explicit id+company_id, full logging). v2_list_companies superadmin GLOBAL. Acceptance: add company → v2_upsert_company 200 success:true → SELECT * FROM companies shows row → refresh shows 3 → other browser sees 3 → no banner."}
