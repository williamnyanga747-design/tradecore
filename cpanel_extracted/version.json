{"version":"1.0.9","name":"TradeCore ERP","date":"2026-09-08","description":"v1.0.9 build 2026-09-08-13 COMPANY-ADD FIX after DIRECT-MYSQL migration: Add Company now bypasses the saveAllData diff dispatch and fires an EXPLICIT awaited v2_upsert_company POST (crypto.randomUUID id, INSERT INTO companies ... ON DUPLICATE KEY UPDATE deleted_at=NULL) followed by a GLOBAL v2_list_companies re-fetch that re-reads MySQL and applies it locally + full console logging ([Direct MySQL] v2_upsert_company OK/FAILED / v2_list_companies re-fetch N rows), so a newly added Company is written straight to the companies table and is visible to ALL users AND survives reload (fixes the local-only bug where the company vanished on refresh / never appeared for other users because the diff-dispatch POST failed silently via .catch(()=>{})). DIRECT_DELTA_HANDLERS catch blocks now log instead of swallowing. Server v2_upsert_company/v2_list_companies unchanged (already correct)."}
